import { LISTA_CONVERSA_USUARIO } from '../Actions/types';;

const INITIAL_STATE = {}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
    
        default:
            return state;

    }
}
